import React from "react";
import { hot } from "react-hot-loader/root";

interface Props {}

const CreateOrJoinGame = (props: Props) => {
  return <div>Create or Join a Game</div>;
};

export default hot(CreateOrJoinGame);
