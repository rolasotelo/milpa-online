export enum CropId {
  Corn = "corn",
}

export enum GoodId {
  Tlaloc = "tlaloc",
  Manure = "manure",
}

export enum ModifierId {
  Wet = "wet",
  Empty = "empty",
}

export enum CardType {
  Crop = "crop",
  Good = "good",
  Empty = "empty",
}

export enum Row {
  First,
  Second,
  Third,
  Fourth,
}

export enum Column {
  First,
  Second,
  Third,
  Fourth,
}

export enum Direction {
  Top,
  Down,
  Left,
  Right,
}
