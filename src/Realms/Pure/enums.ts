export enum CropId {
  Corn = "corn",
}

export enum GoodId {
  Tlaloc = "tlaloc",
  Manure = "manure",
}

export enum ModifierId {
  Wet = "wet",
  Empty = "empty",
}

export enum CardType {
  Crop = "crop",
  Good = "good",
  Empty = "empty",
}

export enum Row {
  Top,
  Down,
  First = 0,
  Second,
  Third,
  Fourth,
}

export enum Column {
  First,
  Second,
  Third,
  Fourth,
  Left = 2,
  Right,
}
